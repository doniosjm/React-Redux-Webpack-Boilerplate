const trip = (state = {}, action) => {
    switch (action.type) {
        case 'TRIP_GET':
            return {
            };
        default:
            return state;
    }
};

export default trip;